node_modules/
.DS_Store
*.log
.cache/
.idea/
.vscode/
dist/
*.tmp

# Local Claude Code state - machine-specific, never for a public repo
.claude/

# Playwright MCP scratch snapshots - regenerated on every run
.playwright-mcp/

# Graphify: keep the report (GRAPH_REPORT.md, graph.html, graph.json),
# drop the machine-specific cache and the absolute-path manifest.
graphify-out/cache/
graphify-out/manifest.json
graphify-out/cost.json
